#include <iostream>
#include <utility>
#include <string>
#include <array>
#include <memory>

	
int main() {
	string tekst;
	tekst='https://leetcode.com/problems/design-tinyurl';
	unique_ptr<TinyUrlCodec> Init();
//	void NextHash(arrray<char, 6> *state);
//	string Encode(const string &url, unique_ptr<TinyUrlCodec> *codec);
//	string Decode(const unique_ptr<TinyUrlCodec> &codec, const string &hash);
	
	return 0;
}
